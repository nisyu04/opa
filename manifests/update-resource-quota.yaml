apiVersion: v1
kind: ResourceQuota
metadata:
  name: my-resource-quota
  namespace: ynishizaki-dev
spec:
  hard:
    requests.cpu: "10"
    requests.memory: 10Gi
    limits.cpu: "10"
    limits.memory: 12Gi
